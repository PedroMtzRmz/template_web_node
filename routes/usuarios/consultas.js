const express = require('express')

const app = express()

//get, post, put, delete

app.post('/usuarios/agregar', (req,res) => {
    // let body = req.body
    // res.json({prop: val})
})

module.exports = app